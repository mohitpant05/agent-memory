# agent-memory

A local, self-hosted semantic memory layer shared between AI coding agents
(Claude Code, Kiro, and any other MCP client).

Everything runs on your own machine. No API key, no cloud, no network egress.

> Status: scaffold. See the commit history for how this is assembled.
